// Public endpoint only. Telegram credentials belong in server secrets.
// Keep disabled until the handler and Telegram destination are configured.
export const orderConfig = Object.freeze({ enabled: false, endpoint: '' });
